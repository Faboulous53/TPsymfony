{% extends 'admin/base.html.twig' %}

{% block title %}SocialNetworks index{% endblock %}

{% block body %}
    <h1>SocialNetworks index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Social_name</th>
                <th>Class_icone</th>
                <th>Social_network_url</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for social_network in social_networks %}
            <tr>
                <td>{{ social_network.id }}</td>
                <td>{{ social_network.socialName }}</td>
                <td>{{ social_network.classIcone }}</td>
                <td>{{ social_network.socialNetworkUrl }}</td>
                <td>
                    <a href="{{ path('app_admin_social_networks_show', {'id': social_network.id}) }}">show</a>
                    <a href="{{ path('app_admin_social_networks_edit', {'id': social_network.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_social_networks_new') }}">Create new</a>
{% endblock %}

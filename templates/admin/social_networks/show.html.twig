{% extends 'admin/base.html.twig' %}

{% block title %}SocialNetworks{% endblock %}

{% block body %}
    <h1>SocialNetworks</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ social_network.id }}</td>
            </tr>
            <tr>
                <th>Social_name</th>
                <td>{{ social_network.socialName }}</td>
            </tr>
            <tr>
                <th>Class_icone</th>
                <td>{{ social_network.classIcone }}</td>
            </tr>
            <tr>
                <th>Social_network_url</th>
                <td>{{ social_network.socialNetworkUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_social_networks_index') }}">back to list</a>

    <a href="{{ path('app_admin_social_networks_edit', {'id': social_network.id}) }}">edit</a>

    {{ include('admin/social_networks/_delete_form.html.twig') }}
{% endblock %}
